#include <iostream>
using namespace std;

int swap(int x, int y) {
    x = x + y;
    y = x - y;
    x = x - y;
    cout << x << y;
}

int main() {
    int a, b;
    cout << "Enter a, b values: ";
    cin >> a >> b;
    swap(a, b);
}
